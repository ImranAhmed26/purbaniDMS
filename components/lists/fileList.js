import React from "react";
import DisplayCard from "../common/displayCard";

const FileList = () => {
  return (
    <div>
      <DisplayCard>File Systems</DisplayCard>
    </div>
  );
};

export default FileList;
